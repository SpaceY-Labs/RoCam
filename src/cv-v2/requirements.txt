# SiamMask-Lite — Training & Inference Requirements
# Install: pip install -r requirements.txt
#
# ── Core ─────────────────────────────────────────────────────────────────
torch>=2.1.0               # Tested on 2.1.x and 2.2.x
torchvision>=0.16.0
numpy>=1.24.0
opencv-python>=4.8.0
pillow>=9.5.0

# ── Training ──────────────────────────────────────────────────────────────
tensorboard>=2.14.0        # Loss/metric visualization
pyyaml>=6.0                # Config file loading
tqdm>=4.65.0               # Progress bars

# ── Data / Annotation ────────────────────────────────────────────────────
pycocotools>=2.0.6         # COCO-format evaluation (also used by your benchmark)

# ── Export ───────────────────────────────────────────────────────────────
onnx>=1.14.0               # ONNX export (needed before TensorRT conversion)
onnxruntime>=1.16.0        # ONNX runtime (for CPU validation before TRT)

# ── Optional: SAM-assisted mask generation ───────────────────────────────
# git+https://github.com/facebookresearch/segment-anything.git
# Download SAM checkpoint: https://dl.fbaipublicfiles.com/segment_anything/sam_vit_b_01ec64.pth

# ── Jetson-specific (install separately on device) ───────────────────────
# tensorrt          (pre-installed on JetPack)
# pycuda            (pip install pycuda on Jetson)
# pyds              (DeepStream Python bindings — already in your pipeline)
