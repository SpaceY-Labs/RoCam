# RoCam Source Code

The folders and files for this project are as follows:

- `./backend` - Backend code running on Jetson
- `./react-app` - Frontend code served by Jetson, viewed by operator on a laptop
- `./benchmark` - Benchmark tools for YOLO models on DeepStream TensorRT
- `./labelling-app` - Labelling app for labelling data for training the YOLO model
- `./pcb-firmware` - STM32 firmware running on gimbal